defmodule EctoTursoEx do
  @moduledoc """
  Entry point for the future Ecto adapter built on top of `TursoEx`.

  The package exists now to keep the repository shape honest while the core
  `turso_ex` package is still being built out.
  """
end

# EctoTursoEx

Ecto adapter package for TursoEx.

This package lives beside `turso_ex` in the same repository and depends on it. It is a sibling package, not an umbrella app.

Current status:

- package scaffolded
- adapter not implemented yet
- should depend on `turso_ex` rather than reshaping the core package around Ecto
